---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<div class="mt-2 flex gap-4">
    <div class="flex flex-col items-stretch justify-center select-none">
        <span class="text-[.4rem] text-gray-400">▼//YUEZONE</span>
        <span class="w-16 pr-2 font-medium text-right align-bottom text-gray-800 bg-gray-200">⇲</span>
    </div>

    <h2 class="text-4xl font-semibold">{title}</h2>
    <slot />
</div>
